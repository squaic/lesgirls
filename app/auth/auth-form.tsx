"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export function AuthForm({next}:{next?:string}){const [signup,setSignup]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=new FormData(e.currentTarget),email=String(data.get("email")),password=String(data.get("password")),firstName=String(data.get("firstName")||"");const sb=createClient();const result=signup?await sb.auth.signUp({email,password,options:{data:{first_name:firstName}}}):await sb.auth.signInWithPassword({email,password});if(result.error){setError(result.error.message);setBusy(false);return}location.href=next||"/"}return <><form onSubmit={submit} className="space-y-3">{signup&&<div><label className="label">Ton prénom</label><input className="field" name="firstName" required autoComplete="given-name" placeholder="Léa"/></div>}<div><label className="label">Adresse e-mail</label><input className="field" name="email" type="email" required autoComplete="email" placeholder="lea@exemple.fr"/></div><div><label className="label">Mot de passe</label><input className="field" name="password" type="password" minLength={6} required autoComplete={signup?"new-password":"current-password"} placeholder="6 caractères minimum"/></div>{error&&<p className="error">{error}</p>}<button disabled={busy} className="btn btn-primary">{busy?"Un instant…":signup?"Créer mon compte":"Se connecter"}</button></form><button onClick={()=>{setSignup(!signup);setError("")}} className="mt-5 w-full text-sm font-bold text-[#e84b72]">{signup?"J’ai déjà un compte":"Première fois ? Créer mon compte"}</button></>}
